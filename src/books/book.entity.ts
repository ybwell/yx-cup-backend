export class Book {
  id: number;
  title: string;
  author: string;
  callNumber: string;
  location: string;
  quantity: number;
  remainingQuantity: number;
  totalQuantity: number; // 添加 totalQuantity 属性
}
